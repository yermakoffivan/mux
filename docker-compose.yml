# Shux server docker-compose configuration
# Legacy service/container/volume identifiers stay stable for existing automation and data.
#
# Quick start:
#   docker compose up -d
#
# Add a project:
#   docker compose run --rm mux-server --add-project /projects/my-repo

services:
  mux-server:
    build: .
    container_name: mux-server
    ports:
      - "3000:3000"
    volumes:
      - mux-data:/root/.mux
      # Mount project directories as needed:
      # - ~/projects:/projects:rw
    restart: unless-stopped

volumes:
  mux-data:
