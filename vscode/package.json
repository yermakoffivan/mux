{
  "name": "mux",
  "displayName": "Shux",
  "description": "Open shux workspaces directly from VS Code",
  "version": "0.1.0",
  "publisher": "coder",
  "icon": "icon.png",
  "extensionKind": [
    "ui"
  ],
  "engines": {
    "vscode": "^1.106.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "shux",
    "mux",
    "workspace",
    "remote",
    "ssh",
    "development"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:mux.chatView",
    "onCommand:mux.openWorkspace",
    "onCommand:mux.configureConnection",
    "onCommand:mux.debugConnection"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mux.openWorkspace",
        "title": "shux: Open Workspace"
      },
      {
        "command": "mux.configureConnection",
        "title": "shux: Configure Connection"
      },
      {
        "command": "mux.debugConnection",
        "title": "shux: Debug Connection"
      }
    ],
    "viewsContainers": {
      "secondarySidebar": [
        {
          "id": "muxSecondary",
          "title": "shux",
          "icon": "icon.png"
        }
      ]
    },
    "views": {
      "muxSecondary": [
        {
          "id": "mux.chatView",
          "name": "Chat (Preview)",
          "type": "webview"
        }
      ]
    },
    "configuration": {
      "title": "shux",
      "properties": {
        "mux.connectionMode": {
          "type": "string",
          "enum": [
            "auto",
            "server-only",
            "file-only"
          ],
          "default": "auto",
          "scope": "machine",
          "description": "How the shux VS Code extension connects to shux (prefer server, require server, or use local files)."
        },
        "mux.serverUrl": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "description": "Override the shux server URL (leave empty to auto-discover). Example: http://127.0.0.1:3000"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "bun run compile",
    "compile": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.106.0",
    "@vscode/vsce": "^2.22.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.3.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/coder/mux.git",
    "directory": "vscode"
  },
  "license": "AGPL-3.0-only",
  "packageManager": "bun@1.1.42"
}
